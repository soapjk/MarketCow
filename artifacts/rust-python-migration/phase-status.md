# Rust + Python migration phase status

Updated 2026-08-28. This is a shadow milestone, not a production migration completion claim.

| Phase | Status | Reproducible evidence | Remaining gate |
|---|---|---|---|
| 0 | Partial | `phase0/inventory.json`, ADR, ownership, baseline, corpus manifest | raw r2 cursor 24906390–25040506 is absent |
| 1 | Shadow skeleton complete | Rust CLI/config/lifecycle/health/metrics; PostgreSQL and ClickHouse typed connection/migration foundations and real-instance tests; UDS handshake | production cutover is intentionally deferred to Phase 7 |
| 2 | Partial | health/readiness/scope/admin/metrics, auth/audit tests, fail-closed snapshot/events/checkpoint plus network WebSocket full-sync/resume/gap tests; bounded Rust MCP transport with native `service_health`, `get_instrument`, `get_quotes` and `get_canonical_bars`, exact 14-tool loopback staged proxy, Python/Rust golden definitions and cross-process real-TCP differential calls | remaining legacy HTTP route compatibility, native/worker-backed replacement of 10 MCP tools and removal of the Python public API proxy |
| 3 | Polymarket sample partial | adapter/normalizer, exact decimal, catalog/lifecycle, single writer, immutable ArcSwap projection, grouped append-only WAL, checkpoint/replay, full-sync/events/WS stream, backpressure and fail-closed tests | raw r2 differential corpus; passing scheduled 45/60-minute and 24-hour shadow/network gates |
| 4 | Partial | workload ownership boundaries and hashed registry; append-only hashed runtime config versions; authenticated CAS-fenced migration checkpoints; fenced PostgreSQL job state; typed PostgreSQL Instrument repository; append-only PostgreSQL audit; typed ClickHouse cached quote and canonical bar/adjustment repositories; native HTTP/MCP reads with exact decimals, signed snapshot cursors and real PostgreSQL 17.10/ClickHouse 25.8 differential/restart tests; disposable SQLite index; Rust content-addressed Artifact promotion and transactional job/manifest registration | actual writer freeze/boundary/single-writer handoff remains disabled; quote/bar writers remain Python-owned; fundamentals/dividends/calendar and CSV/history storage domains; domain-mutation/final-outcome transaction coupling; broader differential/fault tests; disable corresponding Python writers |
| 5 | Partial | UDS 0600 protocol; leased persistent job state/retry/cancel; persistent per-capability concurrency/minimum-interval dispatch policy and one-capability-per-process isolation; capability-specific owner-only secret references delivered only as inherited FD 3; pre-promotion lease authorization; Rust-only hash/path/result-schema verification, promotion and transactional registration; full Python poll/start/complete/fail loop; deterministic SEC filing and bounded CSV inference handlers; opt-in LongPort Instrument resolution handler with FD3-only secrets, exact decimal results and no public port or database credentials; Rust-owned optional worker pool with cleared credentials, bounded restart budgets, CPU/memory/FD/core limits and health/metrics | real-credential provider differential/fault injection, remaining provider handlers, and deletion of Python public API dependency |
| 6 | Partial | provider-neutral Rust realtime crate; shared Python/Rust Hyperliquid golden normalization; exact decimals/source time and exact wire SHA; Rust-owned allow-listed WSS transport with validated subscription acknowledgements, official application heartbeat, bounded output queue/reconnect, local network fault injection, duplicate suppression, bounded replay, contiguous filtered watermarks and explicit expired-gap tests | wire transport to durable WAL/checkpoint and gateway; controlled upstream differential and recovery tests; generic RealtimeHub, LongPort and EastMoney/Sina migration; staged single-writer handoff |
| 7 | Intentionally blocked | guarded non-mutating scripts and runbook | cutover evidence, 24-hour/7-day gates, operator approval |

Real orders remain disabled and Tradude lifecycle management remains prohibited. The guarded
scripts always refuse state changes at this milestone.
